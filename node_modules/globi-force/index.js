var inherits = require('inherits');
var EventEmitter = require('events').EventEmitter;
var d3 = require('d3');
var transform = require('./lib/transform.js');

inherits(GloBIForce, EventEmitter);
module.exports = GloBIForce;

function GloBIForce(opts) {
    if (!(this instanceof GloBIForce)) return new GloBIForce(opts);
    this.opts = opts;
}

GloBIForce.prototype.appendTo = function (target) {
    if (typeof target === 'string') target = document.querySelector(target);
    if (this.opts.json && this.opts.json.length > 0) {
    }
    this.emit('append', target);
};


